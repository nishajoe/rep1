internal class Program
{
    private static void Main(string[] args)
    {
        string str = null;
        Console.WriteLine(str.Length); //str is null
        //A NullReferenceException in C# means you're trying to use a reference
        //that hasn’t been set — i.e., it's null.


    }
}